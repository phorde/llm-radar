# LLM Radar - Environment Configuration Template
# Copy this file to .env and fill with actual values if needed
#
# NOTE: OpenCode Check uses the OpenCode CLI's built-in credential management
# by default. You typically DO NOT need to set these variables unless you want
# to override the OpenCode CLI configuration.

# ============================================================================
# OPENCODE CONFIGURATION
# ============================================================================

# OpenCode API Key (optional - uses ~/.config/opencode/opencode.json by default)
# OPENCODE_API_KEY=your_opencode_api_key_here

# ============================================================================
# LLM PROVIDER API KEYS (Optional Overrides)
# ============================================================================

# OpenAI
# OPENAI_API_KEY=sk-...
# OPENAI_API_BASE=https://api.openai.com/v1

# Anthropic (Claude)
# ANTHROPIC_API_KEY=sk-ant-...

# Google AI
# GOOGLE_API_KEY=...

# Groq
# GROQ_API_KEY=gsk_...

# Cerebras
# CEREBRAS_API_KEY=...

# DeepSeek
# DEEPSEEK_API_KEY=...

# Moonshot AI
# MOONSHOT_API_KEY=...

# OpenRouter
# OPENROUTER_API_KEY=sk-or-...

# ZAI Coding Plan
# ZAI_API_KEY=...

# ============================================================================
# USAGE NOTES
# ============================================================================
#
# 1. This tool primarily relies on OpenCode CLI authentication
#    Configure providers via: opencode auth
#
# 2. Environment variables are only needed for special cases:
#    - Testing with different credentials than OpenCode CLI
#    - CI/CD environments without OpenCode CLI setup
#    - Temporary credential overrides
#
# 3. NEVER commit the actual .env file to version control
#    The .gitignore file is configured to exclude it
#
# 4. For production deployments, use secure secret management:
#    - Docker secrets
#    - Kubernetes secrets
#    - Cloud provider secret managers (AWS Secrets Manager, etc.)
